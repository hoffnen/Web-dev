// SELECTORS
const clInput = document.querySelector(".cl-input");
const clButton = document.querySelector(".cl-button");

// EVENTLISTENERS
clButton.addEventListener("click", addItem);

//Functions

function addItem(event) {
  event.preventDefault();
}
